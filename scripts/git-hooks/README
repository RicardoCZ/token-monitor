本地钩子：与仓库根目录 **`.gitignore` 对齐**，对暂存区与待推送提交中的路径执行 **`git check-ignore`**，命中则拒绝提交/推送（覆盖 **`docs/`**、**`TODO.md`**、**`TOOLS.md`**、**`.cursor/`** 等所有忽略规则；协作细节见本地 **`docs/开发规范.md`**，`docs/` 不入库）。

一次性启用（在本仓库根目录执行）：

  git config core.hooksPath scripts/git-hooks

恢复默认 Git 钩子目录：

  git config --unset core.hooksPath

要求 Git 使用 sh 执行钩子（Git for Windows / WSL / Linux 均可）。
