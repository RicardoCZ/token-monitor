# Token Monitor 待办事项

## ✅ 已完成

### 重构里程碑
- [x] ~~API Key 鉴权功能~~ - ✅ 已完成（Phase 1-6 全部完成）
- [x] ~~服务注册表重构~~ - ✅ T1-T6 全部完成
- [x] ~~usage_snapshots 单轨~~ - ✅ 已完成

### P2 稳定性与可运营化
- [x] P2-1：自动化测试补齐
- [x] P2-2：采集任务化（定时扫描 + 并发控制 + 重试）
- [x] P2-3：告警链路（基于 usage_snapshots + 冷却防抖）
- [x] P2-4：注册表驱动展示 — `frontend/api.html` 动态渲染
- [x] P2-5：运维与数据治理 — `cleanup_history_data.py` + `docs/ARCHITECTURE.md`
- [x] P2-6：告警收口 — `test_alerting_contract.py` + `admin.html` 告警管理 + 运维手册（现 `docs/OPERATIONS.md`）
- [x] P2-7：发布回归与文档 — 现合并为 `docs/OPERATIONS.md`（迁移 / 配置 / 回归清单）

### 文档优化（2026-04-01）
- [x] DEPLOYMENT.md 添加「🚀 快速启动」一键部署区块
- [x] README.md 添加「🚀 快速启动」区块
- [x] 架构图更新为动态扩展样式
- [x] `init_env.sh` / `init_env.ps1` 过时引用修复

### 2026-04-01 完成
- [x] MiniMax 订阅到期时间获取修复（改用 cycle_audio_resource_package API）
- [x] metric_defs 重复问题修复（数据库清理 + seed 改为替换策略）
- [x] 配置名称优化：
  - 账号名 → 配置名称
  - 长度限制 1-20 字符
  - 字符限制（中文、英文、数字、下划线、横线）
  - 唯一性校验（同用户同服务不重名）
  - Cookie 导入时弹窗填写（必填）
- [x] 讯飞重置时间处理（不显示）
- [x] 手机端监控页面优化（移除「最近采集」「历史指标」）
- [x] accounts.html 新建完成
  - 规则列表全局展示（跨账号）
  - 事件列表全局展示 + 分页（每页5条）
  - 账号标识列（规则表和事件表最左侧）
  - 中文状态说明
  - 进入页面直接显示全局数据（不需要先选账号）

### 2026-04-02 完成
- [x] accounts.html 分页切换优化（消除闪烁）
- [x] 事件列表列宽CSS与HTML顺序匹配修复
- [x] 移除"使用率"列，消息列加宽
- [x] 告警状态颜色优化（与账号列表状态同款样式）
- [x] Metric Key 彻底统一（used/total/percent）
  - 讯飞采集统一输出标准字段
  - 前端移除兼容映射
  - 移动端旧字段清理
  - 历史数据迁移清理
- [x] 监控页刷新稳定性增强（刷新中状态/失败提示/防重复）
- [x] 账号名称内联编辑（移除 prompt）
- [x] 同步按钮3秒防抖（按账号独立冷却）
- [x] 详情按钮点击行为（再次点击收起）
- [x] 页面标题修正（Token Monitor - 账号管理）
- [x] 同步时间显示修复（时区问题）
- [x] 刷新按钮样式修复（箭头旋转非三个点）
- [x] 冷却时间/阈值空值校验
- [x] 账号列表行点击不弹详情（仅详情按钮）
- [x] 管理后台邀请码删除功能（405兼容处理）
- [x] 邀请码分页+状态筛选（全部/可用/已用完）
- [x] 所有页面原生 select 替换为自定义下拉组件（解决白光闪烁）
- [x] 讯飞卡片 remain（剩余额度）显示修复
- [x] 规则"已启用"列改为开关样式（toggle）
- [x] 规则"启用状态"表头和下拉框移除，保存默认启用
- [x] 账号提示文字移除（"当前账号：XXX"）
- [x] 选择账号后页面闪烁修复
- [x] 规则保存区域输入框/按钮UI协调优化
- [x] 最近事件列表布局优化（与邀请码列表风格统一）
- [x] 监控页面缓存刷新优化（返回页面直接显示缓存数据）
- [x] 邀请码使用次数下拉框白光修复（自定义组件）
- [x] 阈值/冷却时间输入框样式优化

### 2026-04-04 完成
- [x] `history.html` 历史趋势：ECharts 5（CDN + SRI + fallback）、独立页 24h/7d
- [x] 双 Y 轴：`percent`（左轴 0–100%）、`used`（右轴）；X 轴 `type: 'time'` 铺满查询窗口；`connectNulls: false`
- [x] 状态栏：首尾数据点时间范围 + 覆盖率（分母为查询窗口时长折合小时，向上取整）
- [x] 历史接口请求省略 `metric_key`（与后端可选参数一致，不过滤指标行）

### 2026-04 文档整理
- [x] 运维类 md 合并为单一 `docs/OPERATIONS.md`；根目录 `DEPLOYMENT.md` 保留为索引
- [x] `开发规范.md` 更名为 `docs/DEVELOPMENT.md`；去除与 `HANDOFF_TODAY`/`FRONTEND_PLAN` 重复的独立文件

---


## 🔧 待开发

### 剩余待办（手机端）

| 待办 | 说明 |
|------|------|
| 手机端刷新按钮增加效果提示 | 不知道有没有刷新成功 |
| 手机图标更换 | 当前图标需要更新 |
| 手机欢迎页优化 | 提升美观度 |
| 手机登录页面优化 | 提升美观度 |

### P3-2：历史趋势图 + 模块化（部分完成）
- [x] 趋势图能力：**仅**独立页 `history.html`（不在 `api.html` 内嵌图表；监控页保留顶部导航跳转即可）
- [ ] `api.html` 大脚本模块化（鉴权/请求封装、仪表盘缓存、卡片渲染等；与趋势页解耦）

### P3-3：告警管理增强
- 事件翻页/筛选
- 静默规则

### P3-4：CDP 流程收敛
- extract 灰度切换

### P3-5：运维诊断页（可选）

### P3 补充说明（原独立 `FRONTEND_PLAN` 摘要）

**后端已有、前端仍少用的能力**（可选迭代）：

| 接口 | 功能 | 前端状态 |
|------|------|----------|
| `GET /api/services/{service_id}` | 单服务详情 | 未使用 |
| `POST /api/cdp/extract` | 一次提取 cookie + 页面数据 | 仍多为两步请求 |
| `GET /api/minimax/cookie` | Cookie 状态 | 未直接使用 |
| `GET /api/status`、`/api/health` | 运维诊断 | 未接入页面 |
| `POST /api/clear-cache` | 清除缓存 | 未接入 |

**前端技术选型（P3）**：ECharts + CDN；模块拆分先逻辑后物理；历史趋势**仅** `history.html`（ADR-005）。

**建议执行顺序（参考工时）**：P3-2 模块化 → P3-3 告警增强 → P3-4 CDP 灰度 → P3-5（可选）。

---

## 📝 前端整理方案（待实施）

### 方案：B + C 渐进式

#### 第 1 阶段：单页 Tab 组织
- 保持 setup.html 单页，做「任务导向 Tab」
- Tab 1: CDP 自动获取
- Tab 2: 手动设置
- Tab 3: 登录辅助

#### 第 2 阶段：JS 模块化
```
frontend/js/
├── setup-auth.js
├── setup-cdp.js
├── setup-manual.js
├── setup-state.js
└── setup-ui.js
```

#### 第 3 阶段（可选）：物理拆页

---

## 🚀 可能的优化
- [ ] 讯飞页面也可以尝试从页面抓取截止日期（目前用 API 的 expiresAt）
- [ ] MiniMax 的 JS 匹配表达式可以更健壮
- [ ] 账号连续失败计数，达到阈值后提示"需重新登录"

---

## 已推迟
- API 鉴权改进方案 - 推迟，优先做服务注册表重构

---
*最后更新: 2026-04（文档合并）*
